-- =====================================================================
-- 행사 초대장 — 본문(안내문) 컬럼
-- =====================================================================
-- 목적: 초대장 화면 중앙에 노출할 긴 안내문(편지 형식) 영역 분리.
--      기존 invitation_message 는 1~2줄짜리 짧은 인사말이고,
--      invitation_body 는 줄바꿈 포함 자유 텍스트 (날짜·장소·인사 등).
-- =====================================================================

ALTER TABLE org_events
  ADD COLUMN IF NOT EXISTS invitation_body text;

COMMENT ON COLUMN org_events.invitation_body IS '초대장 본문 안내문 — 줄바꿈 포함 자유 텍스트';
